# TODO

## Programmatic API

Add an API entry point for systems that need to submit an invoice or invoice batch
without using the command line or browser UI. It should invoke the existing
InvoiceFlow workflow and return the same outcomes, reasons, and audit information.

This is a potential future integration path. It is not implemented in the current
prototype.
